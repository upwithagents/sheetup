# Copy file: dependencies into node_modules instead of symlinking them.
# @upwithagents/ui resolves to the sibling ../updesignsystem directory;
# without this, npm symlinks it there, and Turbopack (pinned to this repo's
# root in next.config.ts) refuses to resolve assets through a symlink that
# leaves the project's filesystem root.
install-links=true
